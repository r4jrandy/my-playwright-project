{
  "name": "my-playwright-project",
  "version": "1.0.0",
  "description": "Playwright framework setuo with MCP integration",
  "main": "index.js",
  "scripts": {
    "test:dev": "ENV_NAME=dev playwright test --config=config/test.playwright.config.ts",
    "test:stage": "ENV_NAME=stage playwright test --config=config/test.playwright.config.ts"
  },
  "keywords": [
    "playwright",
    "playwright-mcp",
    "playwright-e2e-tests"
  ],
  "author": "Rajashekar Playwright framework",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@types/node": "^26.5.1",
    "allure-playwright": "3.11.0",
    "chalk": "^6.0.0",
    "dotenv": "^18.0.1"
  }
}
